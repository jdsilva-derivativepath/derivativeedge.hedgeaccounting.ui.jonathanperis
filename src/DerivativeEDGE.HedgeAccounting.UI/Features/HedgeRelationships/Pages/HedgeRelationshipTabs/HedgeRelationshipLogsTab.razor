<div class="hedge-relation-logs-body">
    @if (Logs == null || !Logs.Any())
    {
        <div class="flex flex-col items-center justify-center p-8 text-gray-500">
            <div class="text-center">
                <h3>📋 No Log Data Available</h3>
            </div>
        </div>
    }
    else
    {
        @foreach (var log in Logs)
        {
            <div class="log-container">
                <div class="log-row">
                    <div class="log-indicator-container">
                        @* <i class="fa pull-left log-indicator" aria-hidden="true"></i> *@
                        <h3>Regression Results</h3>
                    </div>
                    <div class="log-card">
                        <div class="log-header">
                            <span>📅 @log.CreatedOn.ToString("MMMM dd, yyyy 'at' hh:mm tt") | ✅ Success: @log.Success | ❌ Errors: @log.Errors</span>
                        </div>
                        <div><strong>Level:</strong> @log.Level | <strong>Key:</strong> @log.Key</div>
                        <div class="log-message">@log.Message</div>
                    </div>
                </div>
            </div>
        }
    }
</div>