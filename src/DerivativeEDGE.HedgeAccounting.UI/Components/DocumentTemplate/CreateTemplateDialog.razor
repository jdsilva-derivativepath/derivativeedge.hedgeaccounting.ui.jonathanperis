@if (Visibility)
{
    <SfDialog Width="500px" Target="DpDialog" ShowBackdrop="true" CssClass="dp-dialog"
              IsModal="true" @bind-Visible="Visibility" ShowCloseIcon="(CreateTemplateForm.Id != Guid.Empty || IsEditMode)" CloseOnEscape="false">
        <DialogTemplates>
            <Header>@HeaderText</Header>
            <Content>
                <EditForm Model="@CreateTemplateForm" OnSubmit="SubmitInitialCreateForm">
                    <div class="msg-with-header">
                        <div class="content-section">
                            <SfMessage Visible="@IsMessageVisible" Severity="MessageSeverity.Error">
                                <h1>@CharacterCountValidation.Title</h1>
                                <p>@CharacterCountValidation.Message</p>
                            </SfMessage>
                        </div>
                    </div>
                    <CustomValidation @ref="@_customValidation" />
                    <DataAnnotationsValidator />
                    <div class="section-row">
                        <div class="dp-inputs">
                            <span class="input-label">Template Name</span>
                            <SfTextBox CssClass="input-textbox" @bind-Value="@CreateTemplateForm.Name" />
                            <ValidationMessage For="@(() => @CreateTemplateForm.Name)"></ValidationMessage>
                        </div>
                    </div>
                    <div class="section-row">
                        <div class="dp-inputs" >
                            <span class="input-label">Description</span>
                            <SfTextBox CssClass="input-multiline" Multiline="true" @bind-Value="@CreateTemplateForm.Description" />
                            <ValidationMessage For="@(() => @CreateTemplateForm.Description)"></ValidationMessage>
                        </div>
                    </div>
                    <div class="dp-buttons text-right space-x-4">
                        @if (CreateTemplateForm.Id != Guid.Empty || IsEditMode)
                        {
                            <SfButton Type="button" CssClass="button-link button-default" Content="Cancel" OnClick="@DlgRenameClose" />
                        }
                        <SfButton CssClass="button-primary button-default" Content="Apply" IsPrimary="true" />
                    </div>
                </EditForm>
            </Content>
        </DialogTemplates>
        <DialogEvents OnClose="@DlgRenameClose"></DialogEvents>
        <DialogPositionData X="center" Y="center"></DialogPositionData>
    </SfDialog>

}